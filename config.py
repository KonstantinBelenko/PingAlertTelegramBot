bot_token = ""

# Enter default admin users here (at least one)
admin_users = [
    
]

# Default links
website_links =[
    "https://www.google.com"
]

# Default schedule (in minutes)
def_schedule = 40

# Full Chrome driver executable path
exec_path = "$HOME/chromedriver"